position()
	position arguments
	z-index  1 unless @z-index

width( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	width params

height( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	height params

top( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	top params

right( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	right params

bottom( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	bottom params

left( args... )
	params = ()
	for n in args
		push( params, ( 'unit' != typeof( n ) ) ? n : unit( n, unit( n ) || 'px' ) )

	left params

box( _top='', _right='', _bottom='', _left='', _width='', _height='' )
	if( _top    != '' ){ top    _top    }
	if( _right  != '' ){ right  _right  }
	if( _bottom != '' ){ bottom _bottom }
	if( _left   != '' ){ left   _left   }
	if( _width  != '' ){ width  _width  }
	if( _height != '' ){ height _height }